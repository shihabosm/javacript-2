/**
 * variable ==>container 
 * */
// number types variable 
var sunglassPrice =80;

// string type variable 
var subject = 'EEE';

// boolean types variable 
var passed = true;

/**
 * NAMING CONVENTION 
 * 1.Single word 
 * 2. no quote 
 * 3.no gap or dash
 * 4. can no start with a number 
 * 5.can not be a keyword 
 * 6.prefer camelCase
 * 
*/
var lastExamResult=89;
/**
 *OPERATION 
 1. +,-,*,/,%,
 2.+=,-=,*=, /= ,%=,
 3.isNaN
 4.null
 5.Number 
 * 
 * */ 
